apiVersion: 1

datasources:
  - name: 'nominal-ds'
    uid: 'nominal-ds'
    type: 'nominaltest-nominalds-datasource'
    access: proxy
    isDefault: false
    orgId: 1
    version: 1
    editable: true
    jsonData:
      baseUrl: ${NOMINAL_BASE_URL:-https://api-staging.gov.nominal.io/api}
    secureJsonData:
      apiKey: ''

apiVersion: 1

datasources:
  - name: 'nominal'
    uid: 'nominal'
    type: 'nominaltest-nominalds-datasource'
    access: proxy
    isDefault: false
    orgId: 1
    version: 1
    editable: true
    jsonData:
      baseUrl: 'https://api-staging.gov.nominal.io/api'
    secureJsonData:
      apiKey: '${NOMINAL_API_KEY}'

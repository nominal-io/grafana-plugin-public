// Paired with the backend `defaultAPIBaseURL` in pkg/plugin/datasource.go.
// CI enforces that both stay in sync (see .github/workflows/with-backend-ci.yml).
export const DEFAULT_NOMINAL_BASE_URL = 'https://api.gov.nominal.io/api';

// User-facing links rendered in the datasource config editor.
export const NOMINAL_PLUGIN_README_URL = 'https://github.com/nominal-io/grafana-plugin-public#readme';
export const NOMINAL_DOCS_URL = 'https://docs.nominal.io';
